<Window x:Class="Budgeter.WPFApplication.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Budgeter.WPFApplication.ViewModels"
        xmlns:controls="clr-namespace:Budgeter.WPFApplication.Views" Loaded="OnLoaded" Closing="OnClosing"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel x:Name="ViewModel"/>
    </Window.DataContext>
    <DockPanel>
        <controls:MainMenu x:Name="Menu" DockPanel.Dock="Top"/>
        <Grid DockPanel.Dock="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ListView Name="YNABList" ItemsSource="{Binding Results}" Grid.Row="0" Grid.Column="0">
                <!--<ListView.Resources>
                    <Style TargetType="{x:Type GridViewColumnHeader}">
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                    </Style>
                </ListView.Resources>-->
                <ListView.View>
                    <GridView x:Name="YNABGrid"/>
                </ListView.View>
            </ListView>
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch"/>
            <ListView Name="BankList" ItemsSource="{Binding Results}"  Grid.Row="0" Grid.Column="2">
                <!--<ListView.Resources>
                    <Style TargetType="{x:Type GridViewColumnHeader}">
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                    </Style>
                </ListView.Resources>-->
                <ListView.View>
                    <GridView x:Name="BankGrid"/>
                </ListView.View>
            </ListView>
            <Button Name="MatchButton" Command="{Binding PerformMatchingCommand}" Content="Perform Matching" Grid.Row="1" Grid.Column="0"/>
            <Button Name="ClearConsoleButton" Command="{Binding ClearConsoleCommand}" Content="Clear Console" Grid.Row="1" Grid.Column="2"/>
        </Grid>
        <controls:LogView x:Name="LogView" DockPanel.Dock="Bottom"/>
    </DockPanel>
</Window>
